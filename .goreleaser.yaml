version: 2

builds:
  - id: dashboard
    binary: sddb-dashboard
    main: ./cmd/dashboard
    goos: [linux, darwin]
    goarch: [amd64, arm64]
    env: [CGO_ENABLED=0]
    ldflags: ["-s -w"]

  - id: agent
    binary: sddb-agent
    main: ./cmd/agent
    goos: [linux]
    goarch: [amd64, arm64]
    env: [CGO_ENABLED=0]
    ldflags: ["-s -w"]

archives:
  - id: dashboard
    ids: [dashboard]
    formats: [tar.gz]
    name_template: "sddb-dashboard_{{ .Version }}_{{ .Os }}_{{ .Arch }}"
    files:
      - LICENSE*
      - README*

  - id: agent
    ids: [agent]
    formats: [tar.gz]
    name_template: "sddb-agent_{{ .Version }}_{{ .Os }}_{{ .Arch }}"
    files:
      - LICENSE*
      - README*

checksum:
  name_template: "checksums.txt"

changelog:
  sort: asc
  filters:
    exclude:
      - "^docs:"
      - "^test:"
      - "^chore:"
